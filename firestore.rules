rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // GEÇİCİ: Test için tüm erişim açık
    // PRODUCTION'DA MUTLAKA DEĞİŞTİRİLMELİ!
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
